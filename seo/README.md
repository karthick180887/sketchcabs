# SERP Analysis Notes

- `serp-analysis.csv` contains the keyword list and a blank `top_10_sites` column.
- Populate `top_10_sites` with the top 10 domains for each keyword from Google Search Console or a SERP data source.
- If you want automation, share a GSC export or API credentials for the data source you prefer.
